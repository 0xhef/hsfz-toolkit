<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ── BMSecResearch permissions ─────────────────────────────────
         INTERNET                       — TCP/UDP socket operations
         ACCESS_NETWORK_STATE           — detect WiFi vs cellular for the
                                          calibration reader's bench-only
                                          host validation
         ACCESS_WIFI_STATE              — read current WiFi SSID/IP for
                                          discovery and connection sanity
         CHANGE_WIFI_MULTICAST_STATE    — required for HSFZ vehicle
                                          discovery (UDP broadcast); without
                                          a held MulticastLock the WiFi
                                          driver drops broadcast packets
                                          in power-save mode
         All four are normal install-time permissions — no runtime prompts.
         See SCOPE.md and ANDROID.md.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.bmsecresearch"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <!--
          NOTE: This file lives under `src-tauri/gen/android/` which is
          regenerated by `tauri android init`. If you delete and
          regenerate the project, reapply the two custom attributes
          below: `android:screenOrientation="portrait"` (locks the
          app to portrait so we don't have to write landscape layouts
          for a workflow that's always single-column tall forms) and
          `android:windowSoftInputMode="adjustResize"` (lets the
          visual viewport shrink when the IME pops up so CSS
          `env(keyboard-inset-height)` is reported correctly).
        -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
